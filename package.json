{
  "name": "hello-world-server",
  "version": "1.0.0",
  "description": "A simple Hello World HTTP server",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
